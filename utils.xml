<krpano>

	<style name="tooltip_hotspot" url="%BASEDIR%/skin/vtourskin_hotspot.png" scale="0.5" edge="top" oy="0" distorted="false" onloaded="add_all_the_time_tooltip();" />
	<action name="add_all_the_time_tooltip">
		txtadd(tooltipname, 'tooltip_', get(name));
		addplugin(get(tooltipname)); txtadd(plugin[get(tooltipname)].parent, 'hotspot[',get(name),']');
		set(plugin[get(tooltipname)].url,'%SWFPATH%/plugins/textfield.swf');
		set(plugin[get(tooltipname)].align,top); set(plugin[get(tooltipname)].edge,bottom);
		set(plugin[get(tooltipname)].x,0); set(plugin[get(tooltipname)].y,0);
		set(plugin[get(tooltipname)].width,200); set(plugin[get(tooltipname)].autoheight,true);
		set(plugin[get(tooltipname)].background,false); set(plugin[get(tooltipname)].border,false);
		set(plugin[get(tooltipname)].css,'text-align:center; color:#FFFFFF; font-family:Arial; font-weight:bold; font-size:16px;');
		set(plugin[get(tooltipname)].textshadow,1); set(plugin[get(tooltipname)].textshadowrange,6.0); set(plugin[get(tooltipname)].textshadowangle,90);
		copy(plugin[get(tooltipname)].html,hotspot[get(name)].tooltip);
		set(plugin[get(tooltipname)].enabled,false);
	</action>

    <action name="do_crop_animation" scope="local" args="framewidth, frameheight, framerate">
		<!-- define local variables -->
		calc(local.xframes, (caller.imagewidth /framewidth) BOR 0);
		calc(local.frames, xframes * ((caller.imageheight / frameheight) BOR 0));
		def(local.frame, integer, 0);
		
		<!-- set the first frame -->
		calc(caller.crop, '0|0|' + framewidth + '|' + frameheight);
		
		<!-- do the animation -->
		setinterval(calc('crop_anim_' + caller.name), calc(1.0 / framerate),
			if(caller.loaded,
				inc(frame);
				if(frame GE frames, if(caller.onlastframe !== null, callwith(caller, onlastframe() ) ); set(frame,0); );
				mod(xpos, frame, xframes);
				div(ypos, frame, xframes);
				Math.floor(ypos);
				mul(xpos, framewidth);
				mul(ypos, frameheight);
				calc(caller.crop, xpos + '|' + ypos + '|' + framewidth + '|' + frameheight);
			  ,
				<!-- stop the interval when the hotspot gets removed -->
				clearinterval(calc('crop_anim_' + caller.name));
			);
		);
	</action>

	<action name="play_animation" scope="private:ANIMATION" args="framewidth, frameheight, framerate, loop">
		ifnot(isset(init),
			calc(xframes, (caller.imagewidth /framewidth) BOR 0);
			calc(frames, xframes * ((caller.imageheight / frameheight) BOR 0));
			def(frame, integer, 0);
			calc(caller.crop, '0|0|' + framewidth + '|' + frameheight);
			def(init,boolean,true);
			def(isruning,boolean,true);
		,
			set(frame,0);
			if(isruning,break(););
		);

		setinterval(calc('crop_anim_' + caller.name), calc(1.0 / framerate),
			if(caller.loaded,
				inc(frame);
				if(frame GE frames, 
					if(caller.onlastframe !== null, 
						callwith(caller, onlastframe() ) 
					); 
					if(loop,
						set(frame,0); 
					,
						clearinterval(calc('crop_anim_' + caller.name));
						set(isruning,false);
						set(frame,calc(frames-1));
					);
				);

				if(frame == caller.eventframe,
					if(caller.onevent !== null,
						callwith(caller, onevent() );
					);
				);
				mod(xpos, frame, xframes);
				div(ypos, frame, xframes);
				Math.floor(ypos);
				mul(xpos, framewidth);
				mul(ypos, frameheight);
				calc(caller.crop, xpos + '|' + ypos + '|' + framewidth + '|' + frameheight);
			  ,
				<!-- stop the interval when the hotspot gets removed -->
				clearinterval(calc('crop_anim_' + caller.name));
				set(isruning,false);
			);
		);
	</action>

	<action name="play_animation1" scope="private:ANIMATION1" args="framewidth, frameheight, framerate, loop">
		
		ifnot(isset(init),
			calc(xframes, (caller.imagewidth /framewidth) BOR 0);
			calc(frames, xframes * ((caller.imageheight / frameheight) BOR 0));
			def(frame, integer, 0);
			calc(caller.crop, '0|0|' + framewidth + '|' + frameheight);
			def(init,boolean,true);
			def(isruning,boolean,true);
		,
			set(frame,0);
			if(isruning,break(););
		);

		setinterval(calc('crop_anim_' + caller.name), calc(1.0 / framerate),
			if(caller.loaded,
				inc(frame);
				if(frame GE frames, 
					if(caller.onlastframe !== null, 
						callwith(caller, onlastframe() ) 
					); 
					if(loop,
						set(frame,0); 
					,
						clearinterval(calc('crop_anim_' + caller.name));
						set(isruning,false);
						set(frame,calc(frames-1));
					);
				);

				if(frame == caller.eventframe,
					if(caller.onevent !== null,
						callwith(caller, onevent() );
					);
				);
				mod(xpos, frame, xframes);
				div(ypos, frame, xframes);
				Math.floor(ypos);
				mul(xpos, framewidth);
				mul(ypos, frameheight);
				calc(caller.crop, xpos + '|' + ypos + '|' + framewidth + '|' + frameheight);
			  ,
				<!-- stop the interval when the hotspot gets removed -->
				clearinterval(calc('crop_anim_' + caller.name));
				set(isruning,false);
			);
		);
	</action>




	<action name="play_animation_like" scope="private:ANIMATIONLIKE" args="framewidth, frameheight, framerate, loop">
		ifnot(isset(init),
			calc(xframes, (caller.imagewidth /framewidth) BOR 0);
			calc(frames, xframes * ((caller.imageheight / frameheight) BOR 0));
			def(frame, integer, 0);
			calc(caller.crop, '0|0|' + framewidth + '|' + frameheight);
			def(init,boolean,true);
			def(isruning,boolean,true);
		,
			set(frame,0);
			if(isruning,break(););
		);

		setinterval(calc('crop_anim_' + caller.name), calc(1.0 / framerate),
			if(caller.loaded,
				inc(frame);
				if(frame GE frames, 
					if(caller.onlastframe !== null, 
						callwith(caller, onlastframe() ) 
					); 
					if(loop,
						set(frame,0); 
					,
						clearinterval(calc('crop_anim_' + caller.name));
						set(isruning,false);
						set(frame,calc(frames-1));
					);
				);

				if(frame == caller.eventframe,
					if(caller.onevent !== null,
						callwith(caller, onevent() );
					);
				);
				mod(xpos, frame, xframes);
				div(ypos, frame, xframes);
				Math.floor(ypos);
				mul(xpos, framewidth);
				mul(ypos, frameheight);
				calc(caller.crop, xpos + '|' + ypos + '|' + framewidth + '|' + frameheight);
			  ,
				<!-- stop the interval when the hotspot gets removed -->
				clearinterval(calc('crop_anim_' + caller.name));
				set(isruning,false);
			);
		);
	</action>


	
</krpano>